import { MatThemeableDirective } from './mat-themeable.directive';

describe('MatThemeableDirective', () => {
  it('should create an instance', () => {
    const directive = new MatThemeableDirective();
    expect(directive).toBeTruthy();
  });
});
