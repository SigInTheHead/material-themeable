import { MatThemeableTextDirective } from './mat-themeable-text.directive';

describe('MatThemeableTextDirective', () => {
  it('should create an instance', () => {
    const directive = new MatThemeableTextDirective();
    expect(directive).toBeTruthy();
  });
});
